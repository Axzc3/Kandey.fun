====================================
AI WORKFLOW INSTRUCTIONS - READ THIS!
====================================

CRITICAL RULE: After EVERY change to files, ALWAYS do these steps:

1. git add -A
2. git commit -m "Descriptive message about what you did"
3. git push origin main

EXAMPLES:
---------
After creating/editing files:
  git add -A
  git commit -m "Added triggerbot feature"
  git push origin main

After deleting files:
  git add -A
  git commit -m "Removed unused overlay files"
  git push origin main

After any code changes:
  git add -A
  git commit -m "Fixed ESP rendering bug"
  git push origin main

DO THIS AUTOMATICALLY - Don't wait for the user to ask!

Repository: https://github.com/Axzc3/Kandey.fun.git

Project Structure:
- core/ - Core DLL functionality (dllmain, entry, offsets, patterns)
- features/ - Game features (ESP, chams, etc)
- main/ - Main utilities (ImGui menu, game storage)

Always keep the Git repo in sync with local changes!
